pipeline

	agent any

	stages {
	stage ('build')
		steps {
			echo "Hello world"
	}
        stage ('TEST')
                steps {
                        echo "Testing"
	 }
        stage ('deploy')
                steps {
                        echo "deploy"
        }
}
